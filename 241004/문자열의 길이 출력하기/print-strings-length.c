#include <stdio.h>
#include <string.h>

int main() {

    char str1[100];
    char str2[100];

    scanf("%s %s", str1, str2);

    printf("%d", strlen(str1) + strlen(str2));



    // 여기에 코드를 작성해주세요.
    return 0;
}